# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: ci-build
'on':
  workflow_dispatch: {}
jobs:
  gradle_build:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_STATE_BUCKET: ${{ secrets.TF_STATE_BUCKET }}
      PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      DISPATCH_GITHUB_TOKEN: ${{ secrets.RUNNER_GITHUB_PAT }}
    timeout-minutes: 40
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.31.3
    - name: Gradle build (in-cluster)
      run: bash scripts/ci/dispatch.sh gradle_build scripts/ci/stages/gradle-build.sh
        2100
  checkstyle:
    runs-on: ubuntu-latest
    needs:
    - gradle_build
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_STATE_BUCKET: ${{ secrets.TF_STATE_BUCKET }}
      PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      DISPATCH_GITHUB_TOKEN: ${{ secrets.RUNNER_GITHUB_PAT }}
    timeout-minutes: 25
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.31.3
    - name: Checkstyle (in-cluster)
      run: bash scripts/ci/dispatch.sh checkstyle scripts/ci/stages/checkstyle.sh
        1500
  pmd:
    runs-on: ubuntu-latest
    needs:
    - checkstyle
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_STATE_BUCKET: ${{ secrets.TF_STATE_BUCKET }}
      PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      DISPATCH_GITHUB_TOKEN: ${{ secrets.RUNNER_GITHUB_PAT }}
    timeout-minutes: 25
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.31.3
    - name: PMD (in-cluster)
      run: bash scripts/ci/dispatch.sh pmd scripts/ci/stages/pmd.sh 1500
  spotbugs:
    runs-on: ubuntu-latest
    needs:
    - pmd
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_STATE_BUCKET: ${{ secrets.TF_STATE_BUCKET }}
      PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      DISPATCH_GITHUB_TOKEN: ${{ secrets.RUNNER_GITHUB_PAT }}
    timeout-minutes: 25
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.31.3
    - name: SpotBugs (in-cluster)
      run: bash scripts/ci/dispatch.sh spotbugs scripts/ci/stages/spotbugs.sh 1500
  junit:
    runs-on: ubuntu-latest
    needs:
    - spotbugs
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_STATE_BUCKET: ${{ secrets.TF_STATE_BUCKET }}
      PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      DISPATCH_GITHUB_TOKEN: ${{ secrets.RUNNER_GITHUB_PAT }}
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.31.3
    - name: JUnit tests (in-cluster)
      run: bash scripts/ci/dispatch.sh junit scripts/ci/stages/junit.sh 1800
  jacoco:
    runs-on: ubuntu-latest
    needs:
    - junit
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_STATE_BUCKET: ${{ secrets.TF_STATE_BUCKET }}
      PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      DISPATCH_GITHUB_TOKEN: ${{ secrets.RUNNER_GITHUB_PAT }}
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.31.3
    - name: JaCoCo coverage (in-cluster)
      run: bash scripts/ci/dispatch.sh jacoco scripts/ci/stages/jacoco.sh 1800
  docker_build:
    runs-on: ubuntu-latest
    needs:
    - jacoco
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_STATE_BUCKET: ${{ secrets.TF_STATE_BUCKET }}
      PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      DISPATCH_GITHUB_TOKEN: ${{ secrets.RUNNER_GITHUB_PAT }}
    timeout-minutes: 35
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.31.3
    - name: Build Docker image (in-cluster)
      run: bash scripts/ci/dispatch.sh docker_build scripts/ci/docker-build.sh 2100
  ecr_push:
    runs-on: ubuntu-latest
    needs:
    - docker_build
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_STATE_BUCKET: ${{ secrets.TF_STATE_BUCKET }}
      PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      DISPATCH_GITHUB_TOKEN: ${{ secrets.RUNNER_GITHUB_PAT }}
    timeout-minutes: 35
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.31.3
    - name: Push image to Amazon ECR (in-cluster)
      run: bash scripts/ci/dispatch.sh ecr_push scripts/ci/ecr-push.sh 2100
  helm_upgrade:
    runs-on: ubuntu-latest
    needs:
    - ecr_push
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_STATE_BUCKET: ${{ secrets.TF_STATE_BUCKET }}
      PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      DISPATCH_GITHUB_TOKEN: ${{ secrets.RUNNER_GITHUB_PAT }}
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.31.3
    - name: Helm upgrade (in-cluster)
      run: bash scripts/ci/dispatch.sh helm_upgrade scripts/ci/helm-upgrade.sh 1800
  rollout_verify:
    runs-on: ubuntu-latest
    needs:
    - helm_upgrade
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_STATE_BUCKET: ${{ secrets.TF_STATE_BUCKET }}
      PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      DISPATCH_GITHUB_TOKEN: ${{ secrets.RUNNER_GITHUB_PAT }}
    timeout-minutes: 25
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.31.3
    - name: Verify rollout (in-cluster)
      run: bash scripts/ci/dispatch.sh rollout_verify scripts/ci/verify-rollout.sh
        1500
